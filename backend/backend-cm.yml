# NOTE (AI assistant upgrade): DB_PASSWORD / MAIL_PASSWORD below are
# plaintext and already committed to source control. They should be
# rotated and moved into the new `backend-secrets` Secret
# (see backend-secret.example.yml) rather than living in a ConfigMap.
# JWT_SECRET (required for the AI assistant's authentication) is
# intentionally NOT here -- it lives only in backend-secrets.
apiVersion: v1
kind: ConfigMap
metadata:
  name: backend-config
data:
  PORT: "5000"
  FLASK_DEBUG: "false"
#if mysql on containrt
  #DB_HOST: "mysql-service"
  #DB_USER: "root"
  #DB_PASSWORD: "root"
  #DB_NAME: "cloud"
#if mysql on RDS
  DB_HOST: "microservices-rds.c89s2s442u7q.us-east-1.rds.amazonaws.com"
  DB_USER: "admin"
  DB_PASSWORD: "Root1234"
  DB_NAME: "cloud"

  MAIL_SERVER: "smtp.gmail.com"
  MAIL_PORT: "587"
  MAIL_USERNAME: "kashifsayyad09@gmail.com" 
  MAIL_PASSWORD: ""
  MAIL_USE_TLS: "true"
  MAIL_USE_SSL: "false"
