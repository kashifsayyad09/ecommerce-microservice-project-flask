# Copy to .env for local development. Never commit a real .env file.
PORT=8100
LOG_LEVEL=INFO

# Internal ClusterIP URL of the existing Flask backend. mcp-server never
# talks to MySQL/RDS directly -- everything goes through this API.
BACKEND_BASE_URL=http://backend-service
BACKEND_TIMEOUT_SECONDS=8
